spring:
  config:
    activate:
      on-profile: local

  batch:
    jdbc:
      initialize-schema: never

  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/insidemovie?useSSL=false&serverTimezone=UTC
    username: root
    password: 12341234

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update # 개발 환경: update

